# -*- mode: makefile-gmake; eval: (when (boundp 'flycheck-mode) (flycheck-mode -1)) -*-

$(eval $(call BEGIN_SRC_LIB_MODULE, symboldata))

$(eval $(call ADD_MODULE_SRC_SOURCES, $(wildcard $(SRCPATH)symboldata/*.cpp)))

$(eval $(call END_SRC_LIB_MODULE))
